__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# SECRETS — this repository is public. Nothing below may ever be committed.
# data/settings.json holds backend api_key values; settings.example.json is the
# placeholder-only version that is safe to track.
data/settings.json
.env
.env.*
*.pem
*.key
*.p12
id_rsa*
id_ed25519*
.git-credentials
secrets.json
**/credentials.json

# runtime data — chats are disposable by decision (§17)
data/tavern.db
data/tavern.db-wal
data/tavern.db-shm
data/music/
data/backgrounds/
data/models/
*.gguf

.DS_Store
data/tavern.log
