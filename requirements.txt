# Termux-friendly: no compiled extras. Do NOT use uvicorn[standard] (§2).
#
# Push notifications need one more, compiled — deliberately NOT installed by
# default any more (§ requirements-push.txt). The one time this pulled
# cryptography in here unconditionally, its Rust extension failed to dlopen
# on a real phone (a PyPI wheel resolved for glibc, not Android's Bionic
# libc — the exact failure `pip install -r requirements.txt` had no reason
# to risk for every install just because one feature might someday want it).
# The app already treats a missing webpush as "notifications unavailable",
# never as a reason not to start (§ config._webpush_vapid) — this file is
# what makes that the *common* case rather than the one-time recovery path.
# Turning notifications on in Settings and using the "Reinstall
# dependencies" button there installs requirements-push.txt on demand.
fastapi>=0.110
uvicorn>=0.27
httpx>=0.26
pydantic>=2.6
